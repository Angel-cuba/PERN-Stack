import { Button } from '@mui/material';
import React from 'react';

export const NavBar = () => {
	return (
		<div>
			NavBar
			<Button variant="contained" color="success">
				Click here
			</Button>
		</div>
	);
};
